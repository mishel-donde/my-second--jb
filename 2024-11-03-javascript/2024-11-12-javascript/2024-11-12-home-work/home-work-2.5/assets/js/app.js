function countNum() {
    sum = 0
    let count = 0;
    for(const row of countNum) {
        for(const cell of row) {
            sum += cell
            count += 1
        }
    }
    
} 


countNum("Hard-Coded " ,3);

let userValue= prompt('enter a value')
let userRandoms=parseInt(prompt('random num'))
displayMessage(userMessage, userTimes);


