
 let number1 = +prompt (`please enter a number`);
 let number2 = +prompt (`please enter another number`);

 // we need to convert להסב each value to the numeric value
 number1 = parseInt(number1);
 number2 = parseInt (number2);

 // if we need to support decimal numbers, we need to use float instead of intgegral

 if(number1 > number2) {
   document.write(`the biggest is ${number1}`)
 } else {
   document.write(` the biggest is ${number2} `)
 }

 // arithmetic equation:
 // 21 > 9 => false

 21
 22
 12
 9

